import React from 'react';
import './AboutSection.scss';

const AboutSection = (props) => (
  <div className="about-section" id="aboutSection">
    <h1 className="about-section__header">About Me</h1>
  </div>
);

export default AboutSection;
