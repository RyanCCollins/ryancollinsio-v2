import React from 'react';
import { Column, Row } from 'react-foundation';

const ResumeSection = ({
  resumeData
}) => (
  <section className="resume-section section-content">
    <h1 className="section-header">Resume</h1>
  </section>
);

export default ResumeSection;
